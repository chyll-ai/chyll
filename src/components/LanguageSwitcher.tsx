
import React from 'react';

/**
 * LanguageSwitcher component
 * 
 * This is a placeholder component that returns null since the site is French-only.
 * No language switching functionality is needed.
 */
const LanguageSwitcher = () => {
  return null;
};

export default LanguageSwitcher;
